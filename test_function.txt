import math;

[%] Simple function to add numbers
function @add(int x) {
    return x + 5;
}

[%] Main function with basic operations
function main() {
    int x = 10;
    
    [%] Test function call
    int result = @add(x);
    
    [%] Test output
    EXERT> (result);
    
    [%] Test simple if statement
    IF (result > 10) {
        EXERT> (1);
    } ELSE {
        EXERT> (0);
    }
}
$$ 